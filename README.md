# Ajay — Software Developer Portfolio

A minimalist React + Vite portfolio.

## 🚀 Quickstart
```bash
npm install
npm run dev
```

## 🔧 Build
```bash
npm run build
npm run preview
```

## ✏️ Customize
- Update text in `src/components/*`.
- Add projects in `src/data/projects.js`.
- Replace contact links in `Contact.jsx`.
- Drop your PDF resume into `public/` and rename to `Ajay-Resume.pdf`.
